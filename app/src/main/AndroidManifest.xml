<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seulgicell">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name = ".SecondPageActicity"/>

        <activity android:name = ".third_page"/>

        <activity android:name = ".fourth_page"/>

        <activity android:name = ".fifth_page"/>

        <activity android:name = ".sixth_page"/>

        <activity android:name = ".seventh_page"/>

        <activity android:name = ".PopupFood"
            android:theme="@style/Theme.MyDialog" />

        <activity android:name = ".PopupMovie"
            android:theme="@style/Theme.MyDialog" />

        <activity android:name = ".PopupShopping"
            android:theme="@style/Theme.MyDialog" />
    </application>

</manifest>